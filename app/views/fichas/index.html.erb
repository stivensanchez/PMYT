<%- model_class = Ficha -%>
<div class="well">
    <h1><center>Fichas <small>Tecnicas</small></center></h1>
</div>
<div class="well">
    <%= link_to 'Nueva Ficha',
            new_ficha_path,
            :class => 'btn btn-primary btn-lg active' %>
</div>
<table class="table table-bordered table-striped">
  <thead>
    <br>
    <tr>
      <th><%= model_class.human_attribute_name(:referencia) %></th>
      <th><%= model_class.human_attribute_name(:nombre) %></th>
      <th><%= model_class.human_attribute_name(:hoja_de_costos) %></th>
      <th><%= model_class.human_attribute_name(:acciones) %></th>
    </tr>
  </thead>
  <tbody>
    <% @fichas.each do |ficha| %>
      <tr>
        <td><%= link_to ficha.referencia, ficha_infos_path(ficha), :class => 'btn btn-default btn-xs btn-lg active' %></td>
        <td><%= ficha.nombre %></td>
        <td><%= link_to 'HOJA DE COSTOS', ficha_costs_path(ficha), :class => 'btn btn-default btn-xs btn-lg active' %></td>
        <td>
          <%= link_to 'Editar',
                      edit_ficha_path(ficha), :class => 'btn btn-default btn-xs btn-lg active' %>
          <%= link_to 'Elimnar',
                      ficha_path(ficha),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger btn-lg active' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


