<%- model_class = Material -%>
<div class="well">
    <h1><center>Materiales <small>Insumos</small></center></h1>
</div>
    <div class="well">
        <%= link_to 'Nuevo', 
            new_info_material_path(@info),
            class: 'btn btn-success btn-lg active' %>
</div>
<table class="table table-bordered table-striped table-hover table-condensed">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:Nombre_Material) %></th>
      <th><%= model_class.human_attribute_name(:Cantidad_Consumo) %></th>
      <th><%= model_class.human_attribute_name(:Observaciones) %></th>
       <th><%= model_class.human_attribute_name(:Acciones) %></th>
  </thead>
  <tbody>
    <% @materials = @info.materials.all.each do |material| %>
      <tr>
        <td><%= material.nombre %></td>
        <td><%= material.cantidad %></td>
        <td><%= material.observaciones %></td>
         <td><center>
         <div id="wrap">
            <ul id="navbar">
              <li>
                <a class="btn btn-default dropdown-toggle" data-toggle="dropdown" href="#">
                ︾</a>
            <ul>
                <li> <%= link_to '<i class="fa fa-wrench txt-primary"></i><span> Editar</span>'.html_safe, edit_info_material_path(@info,material) %></li>
                <li> <%= link_to '<i class="fa fa-times txt-danger"></i><span> Eliminar</span>'.html_safe, info_material_path(@info,material), method: :delete, data: { confirm: 'Estas segur@?' }%></li>
              </ul>
              </li>
              </ul>
          </div>
        </center></td>
      </tr>
    <% end %>
  </tbody>
</table>


