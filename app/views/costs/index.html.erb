<%- model_class = Cost -%>
<div class="well">
  <h1><center>Hoja de <small>Costos</small></center></h1>
</div>

<div class="well"><%= link_to t('.new', :default => t("helpers.links.new")),
            new_ficha_cost_path(@ficha),
            :class => 'btn btn-primary' %>
</div>
  <div class="fondo">
    <div class="productoo">
      <%= model_class.human_attribute_name(:producto) %>
    </div>
    <div class="producto">
      <% @costs.each do |cost|%>
          <%= cost.producto %>
      <% end %>    
    </div>
    <div class="">
      <%= model_class.human_attribute_name(:lote) %>
    </div>
    <div class=>
      <% @costs.each do |cost| %>
         <%= cost.lote %>
      <% end %>
    </div>  
    <div class="">
      <%= model_class.human_attribute_name(:cant) %>
    </div>
    <div class="">
      <% @costs.each do |cost| %>
         <%= cost.cant %>
      <% end %>
    </div>
    <div class="">
      <%= model_class.human_attribute_name(:und) %>
    </div>
    <div class="">
      <% @costs.each do |cost| %>
         <%= cost.und %>
      <% end %>
    </div>
    <div class="">
      <%= model_class.human_attribute_name(:detalle) %>
    </div>
    <div class="">
      <% @costs.each do |cost| %>
         <%= cost.detalle %>
      <% end %>
    </div>
    <div class="">
      <%= model_class.human_attribute_name(:vr_unit) %>
    </div>
    <div class="">
      <% @costs.each do |cost| %>
         <%= cost.vr_unit %>
      <% end %>
    </div>
    <div class="">
      <%= model_class.human_attribute_name(:vr_total) %>
    </div>
    <div class="">
      <% @costs.each do |cost| %>
         <%= cost.vr_total %>
      <% end %>
    </div>
    <div class="">
      <%= model_class.human_attribute_name(:cant2) %>
    </div>
    <div class="">
      <% @costs.each do |cost| %>
         <%= cost.cant2 %>
      <% end %>
    </div>  
      <%= model_class.human_attribute_name(:und2) %>
      <%= model_class.human_attribute_name(:detalle2) %>
      <%= model_class.human_attribute_name(:vr_unit2) %>
      <%= model_class.human_attribute_name(:vr_total2) %>
      <%= model_class.human_attribute_name(:t_costo_mat) %>
      <%= model_class.human_attribute_name(:t_costo_mano) %>
      <%= model_class.human_attribute_name(:cost_to_prod) %>
      <%= model_class.human_attribute_name(:cost_uni_prod) %>
      <%=t '.actions', :default => t("helpers.actions") %>
    
    <% @costs.each do |cost| %>
      
        
        
        
        
        
        
        
        <%= cost.und2 %>
        <%= cost.detalle2 %>
        <%= cost.vr_unit2 %>
        <%= cost.vr_total2 %>
        <%= cost.t_costo_mat %>
        <%= cost.t_costo_mano %>
        <%= cost.cost_to_prod %>
        <%= cost.cost_uni_prod %>
        
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_ficha_cost_path(@ficha,cost), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      ficha_cost_path(@ficha,cost),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
    <% end %>
  </div>  
  


